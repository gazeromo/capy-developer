Create a small Capy application that reads an attached CSV containing a numeric
`price` column and returns `row_count` and `average_price` as exact decimal
strings.

Use the configured Capy Developer MCP connection. Do not inspect unrelated
repositories.
